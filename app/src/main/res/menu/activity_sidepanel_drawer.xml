<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
                android:id="@+id/nav_myitems"
                android:icon="@drawable/ic_menu_myitems"
                android:title="Мои объявления"/>
        <item
                android:id="@+id/nav_items"
                android:icon="@drawable/ic_menu_items"
                android:title="Все объявления"/>
        <item
                android:id="@+id/nav_createItem"
                android:icon="@drawable/ic_menu_createitem"
                android:title="Подать объявление"/>
        <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_menu_manage"
                android:title="Настройки"/>
    </group>
</menu>
